export declare class Transaction<Init = Record<string | number, any>, State = Init | Record<string | number, any>> {
    state: Record<string | number, any>;
    private _resolve;
    private _reject;
    constructor(state?: Init & State);
    private _promise;
    get promise(): Promise<{
        transaction: Transaction<Init, State>;
        data: any;
    }>;
    setState(state: Init | State): void;
    resolve(data?: any): Error | undefined | void;
    reject(error?: any): Error | undefined | void;
}

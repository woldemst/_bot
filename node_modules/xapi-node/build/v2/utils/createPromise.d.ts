export type PromiseObject<T, E = Error> = {
    resolve: (data: T) => void;
    reject: (err: E) => void;
    promise: Promise<T>;
};
export declare function createPromise<T, E = Error>(): PromiseObject<T, E>;

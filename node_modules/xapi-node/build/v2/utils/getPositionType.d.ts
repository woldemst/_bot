import { CMD_FIELD, PositionType } from '../interface/Enum';
export declare function getPositionType({ cmd, closed, close_time, }: {
    cmd: CMD_FIELD;
    closed: boolean;
    close_time: number;
}): PositionType;
